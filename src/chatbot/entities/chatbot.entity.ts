/**
 * Entité TypeScript minimale — générée par NestJS CLI, actuellement vide.
 * Les données du chatbot sont stockées dans les tables `conversations` et `messages`
 * de Prisma, partagées avec le module de messagerie (type='chatbot' pour les distinguer).
 * Les types internes du service sont définis dans chatbot.types.ts et chatbot.service.ts.
 */
export class Chatbot {}
