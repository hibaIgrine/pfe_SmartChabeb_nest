/**
 * DTO de création de notification — généré par NestJS CLI, actuellement vide.
 * Les notifications ne sont jamais créées via une route HTTP directe.
 * Elles sont créées en interne par NotificationsService via des méthodes
 * dédiées (createMembershipDecisionNotification, createEventUpdateNotification, etc.)
 * appelées depuis les autres services de l'application.
 */
export class CreateNotificationDto {}
